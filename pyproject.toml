[project]
name = "my-project"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "requests",
    "python-dotenv",
    "mcp>=1.27.0,<2.0.0",
]

[dependency-groups]
dev = [
    "pytest",
]

[tool.uv]

